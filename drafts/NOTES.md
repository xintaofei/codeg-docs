# Preserved drafts

These are the earlier, code-grounded page drafts. They are **excluded from the
built site** (`srcExclude: ['drafts/**']`). Mine them for real content when
filling in the new framework, then delete when no longer needed.
